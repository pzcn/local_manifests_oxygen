<?xml version="1.0" encoding="UTF-8"?>
<manifest>
    <!-- Remotes -->
    <!-- 新项目均托管在 github.com/kyasu 下，因此这里添加一个对应的 remote -->
    <remote name="kyasu" fetch="https://github.com/kyasu" clone-depth="1" />

    <!-- Device -->
    <!-- sdm660-common 设备公共部分 -->
    <project path="device/xiaomi/sdm660-common"
             name="android_device_xiaomi_sdm660-common"
             remote="kyasu"
             revision="lineage-22.1" />

    <!-- clover 设备部分 -->
    <project path="device/xiaomi/clover"
             name="android_device_xiaomi_clover"
             remote="kyasu"
             revision="lineage-22.1" />

    <!-- Kernel -->
    <project path="kernel/xiaomi/sdm660"
             name="android_kernel_xiaomi_sdm660"
             remote="kyasu"
             revision="lineage-22.1" />

    <!-- Vendor -->
    <!-- Vendor 公共部分 -->
    <project path="vendor/xiaomi/sdm660-common"
             name="android_vendor_xiaomi_sdm660-common"
             remote="kyasu"
             revision="lineage-22.1" />

    <!-- Vendor clover 部分 -->
    <project path="vendor/xiaomi/clover"
             name="android_vendor_xiaomi_clover"
             remote="kyasu"
             revision="lineage-22.1" />
</manifest>
